version: '3.8'

services:
  web:
    build: .
    container_name: advanced-puzzle-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=1
      - SECRET_KEY=dev-secret
    volumes:
      - .:/app